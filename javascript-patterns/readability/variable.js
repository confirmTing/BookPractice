const a = 1;
const b = 2;
const c = 3;

function addA(num) {
  return num + a;
}

function addC(num) {
  return num + c;
}

function addABC(num) {
  return num + a + b + c;
}

const a = 1;
function addA(num) {
  return num + a;
}
